temporalio_cron.execute:
  path: '/temporalio/cron/execute'
  defaults:
    _controller: '\Drupal\temporalio_cron\Controller\ExecuteController::execute'
  requirements:
    _permission: 'access content'
  methods: [POST]
